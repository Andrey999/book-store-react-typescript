import React, { Fragment } from 'react';
import { Auth } from '../components/auth/Auth';

export const AuthPage = () => {
    return(
        <Fragment>
            <Auth />
        </Fragment>
    )
}