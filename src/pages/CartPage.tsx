import React, { Fragment } from 'react';
import { BookCartTable } from '../components/bookCartTable/BookCartTable';

export const CartPage = () => {
    return (
        <Fragment>
            <BookCartTable />
        </Fragment>
    );
};